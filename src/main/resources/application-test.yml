spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/timetable_bot_test
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

admin:
  auth:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin123}
    token: ${ADMIN_TOKEN:dev-admin-token}